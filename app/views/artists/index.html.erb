<!-- <p id="notice"><%= notice %></p> -->

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Followers</th>
      <th>Country of origin</th>
      <th>Tier 1</th>
      <th>Tier 2</th>
      <th>Tier 3</th>
      <th colspan="3"></th>
    </tr>
  </thead> -->

<% @artists.each do |artist| %>

<div class="artists_outer" id="artist-<%=artist.id%>">
    <div class="artists_inner">
        <a class="artists_name" href="artists/<%=artist.id%>"><%= artist.name %></a>
    </div>
</div>
<div class="artists_bottom_box">
    <div class="artists_bottom followers"><%=artist.followers%> followers</div>
    <div class="artists_bottom raised">$ <%=artist.amount_raised%> raised</div> 
    <a class="follow artists_buttons btn btn-lg btn-primary" id="artist_<%= artist.name %>" href="artists/<%=artist.id%>/follow"><i class="fa fa-thumbs-o-up"></i>Follow</a>
    <a class="bring artists_buttons btn btn-lg btn-success" href="artists/<%=artist.id%>/bring">Bring Them Over!</a>
</div>
<!-- 
  <tr>
    <td><%= artist.name %></td>
    <td><%= artist.description %></td>
    <td><%= artist.followers %></td>
    <td><%= artist.country_of_origin %></td>
    <td><%= artist.tier_1 %></td>
    <td><%= artist.tier_2 %></td>
    <td><%= artist.tier_3 %></td>
    <td><%= link_to 'Show', artist %></td>
    <td><%= link_to 'Edit', edit_artist_path(artist) %></td>
    <td><%= link_to 'Destroy', artist, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr> -->
<% end %>

<br>

<!-- <%= link_to 'New Artist', new_artist_path %> -->
